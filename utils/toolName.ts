export enum ToolName {
	SWAP_TOOL = 'swap',
	BRIDGE_TOOL = 'bridge',
	TOKEN_TOOL = 'token',
	WALLET_TOOL = 'wallet',
}

export enum AgentType {
	TOOLS_AGENT = 'toolsAgent',
	PLANNING_AGENT = 'planningAgent',
}
